export function ElasticsearchWorkspaceNotice() {
  return (
    <section className="rounded-xl border border-dashed border-border-default bg-bg-subtle p-4 text-body-secondary text-text-secondary">
      The Elasticsearch connector is available for managing cluster indices and
      running search queries. Select an index or open the Query Console from the
      sidebar.
    </section>
  )
}
