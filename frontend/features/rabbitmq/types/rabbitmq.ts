/**
 * RabbitMQ management-API response shapes.
 *
 * Scaffolding for the RabbitMQ connector (AMQP management API). These shapes
 * are placeholders until the backend connector lands; they are typed as
 * `Record<string, never>` so the empty shapes stay lint-clean and arbitrary
 * values cannot sneak in before real fields are defined.
 */
export type RabbitMQInfo = Record<string, never>

export type RabbitMQQueueInfo = Record<string, never>

export type RabbitMQExchangeInfo = Record<string, never>

export type RabbitMQVirtualHostInfo = Record<string, never>
